Module Square
    Sub Main()
        Dim Edge As Double = Edge
        Dim point1 As New Point(Edge, Edge）
        Dim point2 As New Point(-sqrt(3)/2*Edge, 0)
        Dim point3 As New Point(0, -sqrt(3)/2Edge)
    End Sub
End Module
