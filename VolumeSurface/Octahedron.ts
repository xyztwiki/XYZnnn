// 正八面体
function Volume(Edge: number){
    return (2 * Math.sqrt(2)) / 3 * Math.pow(Edge, 3);
}
function Surface(Edge: number){
    return 2 * Math.sqrt(3) * Math.pow(Edge, 2);
}
   const Edge = NaN;
